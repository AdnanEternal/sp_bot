
# StackHost configuration file

# Version: 2.0.0

user:
  name: "AdnanEternal"
  github: "AdnanEternal"

runtime:
  # Supported runtimes: python, node, go, rust, ruby, java, php, dotnet, elixir, perl, swift
  image: python:3.11-slim # e.g., python:3.11-slim or node:22
  
commands:
  build:
    - "pip install splusthon"
    - "pip install openai"
    - "pip install requests"
  start: "python sorush_bot.py"

repository:
  branch: 'main'
  auto_deploy: false

env: